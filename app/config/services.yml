# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value


services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'

#
    services:
        class: Backend\AdminBundle\Services\Services
        autowire: true

    http.utils.class:
        class: Symfony\Component\Security\Http\HttpUtils
        autowire: true

#    security.authentication.success_handler:
#        class: "%security.authentication.success_handler.class%"
#        public: false
#        autowire: true

    Backend\AdminBundle\EventSubscriber\RedirectToPreferredLocaleSubscriber:
        arguments:
            $locales: ['%app_locales%']
            $defaultLocale: ['%locale%']
        #arguments: ['%kernel.default_locale%']
        # redundant if you're using autoconfigure
        #tags: [kernel.event_subscriber]

    mail_manager:
        class:     Backend\AdminBundle\Lib\MailManager
        arguments: ["@mailer", "@twig"]
        public: true


    #backend_user.registration.form.type:
    #    class: Backend\MyFOSUserBundle\Form\Type\RegistrationFormType
        #autowire: true
    #    arguments: [%fos_user.model.user.class%]
        #tags:
        #    - { name: form.type, alias: backend_user_registration }

    #backend_user.registration_listener:
    #    class: Backend\MyFOSUserBundle\EventListener\RegistrationListener
    #    autowire: true
        #arguments: ['@security.context', '@doctrine', '@service_container']
        #tags:
        #    - { name: kernel.event_subscriber }
